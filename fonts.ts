import { Inter as MyInter, Lusitana } from 'next/font/google';

export const inter = MyInter({ subsets: ['latin'] });
export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});
